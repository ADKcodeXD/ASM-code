DATAS SEGMENT
    DB 'word',0
    DB 'unix',0
    DB 'wind',0
    DB 'good',0  
DATAS ENDS

STACKS SEGMENT
    DB 8 DUP(0)
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    MOV CX,4
  WL:PUSH CX
  	CALL DAXE
  	POP CX
  	ADD SI,1
  	LOOP WL
  	MOV AH,4CH
    INT 21H
DAXE:
	MOV CH,0
	MOV CL,[SI]
	JCXZ A
	AND BYTE PTR [SI],11011111B
	INC SI
	LOOP DAXE
   A:RET	
CODES ENDS
    END START
